require.config({
                paths: {
                	
                    
                    'jquery': 'jquery',
                    'app': 'validacion',
                    'root':'root'

                }
            });
require(['app'], () =>{
});